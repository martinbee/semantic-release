{
  "name": "semantic-test",
  "description": "Test of semantic release. Not a valid package. Will be unpublished.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "author": "",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/martinbee/semantic-release.git"
  },
  "devDependencies": {
    "semantic-release": "^4.3.5"
  }
}
